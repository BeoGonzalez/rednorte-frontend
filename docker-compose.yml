services:
  rednorte-frontend:
    build:
      context: .
      dockerfile: Dockerfile.dev
    container_name: rednorte-frontend
    ports:
      - "4200:4200"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - CHOKIDAR_USEPOLLING=true